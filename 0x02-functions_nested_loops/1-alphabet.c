#include <main.h>
/**
 * main - check the code
 * Description 'print alphabet in correct sequence'
 * Return: Always 0
 */
void print_alphabet(void)
{
	char f:
		for (f = 'a'; f <= 'z'; f++)
		{
			putchar(f);
		}
	putchar ('\n');
	return (0);
}
