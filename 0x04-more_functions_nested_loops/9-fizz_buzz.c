#include <stdio.h>
/**
 * main - start 
 * Description : numbers 1 to 100 are printed out
 * fizz mutiples of 3
 * buzz of 5
 * Return: 0
 */

int main(void)
{
	int d;
	for (d = 1; d <= 100; d++)
	{
		if (d % 15 == 0)
			printf("Fizzbuzz");
		else if (d % 3 == 0)
			printf("Fizz");
		else if (d % 5 == 0)
			printf("Buzz")
		else
			printf("%d", d );
		if (i < 100)
	}
	print ("\n");
		return (0);
}
