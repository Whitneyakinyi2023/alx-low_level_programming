#include "main.h"

/**
 * _isupper - Checks for uppercase
 * @c :character to be assessed
 * Descriptrion: Checking for upper case character
 * Return: 1 for  uppercase or 0 if not
 */

int _isupper(int c)

{

{
	if (c <= 90 && c >= 65)
	{
		return (1);
	}
		return (0);
}
}
