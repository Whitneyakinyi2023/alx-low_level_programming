#include "main.h"
/**
 * _isupper - Checks for uppercase
 * @c :character to be assessed
 * Returns : 1 if c is uppercase 0 if not
 */

int _isupper(int c)

{

{
	if (c <= 90 && c >= 65)
	{
		return (1);
	}
		return (0);
}
}
